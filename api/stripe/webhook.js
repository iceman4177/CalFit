// /api/stripe/webhook.js
// Legacy alias kept for backward compatibility.
// Canonical production webhook handler lives at /api/stripe-webhook.js.
export { config } from "../stripe-webhook.js";
export { default } from "../stripe-webhook.js";
